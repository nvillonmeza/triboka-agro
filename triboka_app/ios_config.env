# TRIBOKA iOS Configuration
# ========================

# Bundle Identifier
BUNDLE_ID = "com.triboka.tribokaApp"

# App Name
APP_NAME = "TRIBOKA"

# Version Configuration
VERSION_NUMBER = "1.0.0"
BUILD_NUMBER = "1"

# iOS Deployment Target
IOS_DEPLOYMENT_TARGET = "13.0"

# Signing Configuration (for future use)
# TEAM_ID = "YOUR_TEAM_ID"
# PROVISIONING_PROFILE = "YOUR_PROVISIONING_PROFILE"

# Firebase Configuration
# FIREBASE_CONFIG_FILE = "ios/Runner/GoogleService-Info.plist"

# Build Configuration
XCODE_PROJECT = "ios/Runner.xcodeproj"
XCODE_WORKSPACE = "ios/Runner.xcworkspace"
XCODE_SCHEME = "Runner"

# Export Options
EXPORT_METHOD = "ad-hoc"  # development, ad-hoc, app-store, enterprise

# Notifications Configuration
FIREBASE_MESSAGING_ENABLED = true
PUSH_NOTIFICATIONS_ENABLED = true